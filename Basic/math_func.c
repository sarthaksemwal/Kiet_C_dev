#include <stdio.h>
#include <math.h>
//program starts execution form here

int main() {
    float r;
    //using the function 
    r=sqrt(2.0);
    printf("the squar root of 2 is %d\n",r);
    
    float s;
    //using the function 
    
    s=pow(2.0,8.0);
    printf("2.0 to the power 8.0 is %d",s);
    return 0;
}

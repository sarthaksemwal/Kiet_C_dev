#include <stdio.h>

int main() {
    const char *message = "Hello, world!";
    
    // Printf is a function that we use to print the output in the screen
    printf("Hello"\n);
    //here \n is  a escape sequence that is used to add a new line
    printf("World");
    
    return 0;
}

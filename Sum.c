#include <stdio.h>

int main() {
    //declaring the variable a,b,sum where sum stores the value of a+b
    int a, b, sum;
    a = 10;
    b = 20;
    sum = a + b;
    //printing the output
    printf("sum is: %d\n", sum);
    return 0;
}
